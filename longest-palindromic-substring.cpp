class Solution {
public:
    string longestPalindrome(string s) {
        // Brute force in O(n^3)
        // 1. Create all possible substrings in a matrix
        // 2. Expand around the center for each to verify
        // TODO: Implementation
    }
};
